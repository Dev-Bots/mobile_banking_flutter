import 'package:flutter/material.dart';

class ClientSavedPages extends StatelessWidget {
  const ClientSavedPages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is the Client Saved Pages"),
      ),
    );
  }
}
